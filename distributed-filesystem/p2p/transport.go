package p2p

// Peer is and interface that represents a node in the network.
type Peer interface {
}

// Handler for comunication between nodes
//form(TCP, UDP, Websockets, etc.)
type Transport interface {
}

